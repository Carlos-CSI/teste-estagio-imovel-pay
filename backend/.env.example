# Database connection URL for Prisma
# Format: mysql://<user>:<password>@<host>:<port>/<database>
#
# <user>     - MySQL username (e.g. root, test_user)
# <password> - MySQL password for the given user
# <host>     - database host (use localhost when running outside Docker)
# <port>     - MySQL port (default: 3306)
# <database> - database name (must match MYSQL_DATABASE in the root .env)
DATABASE_URL="mysql://root:root_password@localhost:3306/cobranca_db"

# Port on which the backend server will listen
PORT=3000